/*const { contextBridge, ipcRenderer } = require('electron');

contextBridge.exposeInMainWorld('robotjs', {
    moveMouse: (data) => ipcRenderer.send('mouseMove', data),
    mouseToggle: (data) => ipcRenderer.send('mouseToggle', data),
});*/